// First Theater venue config for the shared stage engine (Kernel 72).
// This is the plain template venue: default engine behavior, no game layer.
// Copy this file (not the engine) to seed a new game venue.
window.VictoryStageVenue = {
  slug: "first-theater",
  name: "First Theater",
};

# Kernel 93 — Audience Seat Dress Rehearsal Ledger

**Kernel spec:** `Construction/Kernels/Kernel 93 — Audience Seat Dress Rehearsal.md`
**Status:** **KERNEL 93 PASSES IN FULL, per Grant 2026-08-30** ("I just finished End, everything is green, 93 passes."). All four passes (A/B/C/D) are complete — see §6 for the closing summary. **Pass A: COMPLETE, per Grant 2026-08-28** ("I do believe 93 Pass A is finally complete"), following his 2026-08-28 in-progress verdict ("I believe 93 pass A passes") plus a further live round that day: A19's recipient-fallback query hit a real bug (`session_participants` has no `created_at` column, only `joined_at` — fixed in both `audiencenotes.Submit` and Kernel 11's own `resolveNoteCardRecipient`, the latter a pre-existing latent bug this surfaced), a second real bug (the fallback only matched `role = 'director'`, so a Producer running the show alone got `no_director_available` despite outranking Director — widened to accept either, preferring Producer), and the Mailbox link was relocated from a dead, always-hidden info-booth button to Trailers' launchbar and the Account page. See §5 for the full list of bugs found and fixed live across the whole pass. A13 (Configurator Mode) was exercised live extensively (2026-08-24 through 2026-08-26) with real data-loss bugs found and fixed (A22–A27); A14 (Kessa/the door) confirmed resolved by Grant 2026-08-27. A18 (reactions) built 2026-08-26, refined live 2026-08-27 (collapsible/positioning/dock-to-corner) — Grant considers it solid/done. A15 (Kessa not removable) fixed 2026-08-27, not independently re-confirmed live (no report back after the fix). A19 (Audience → Director notes, "Send Fanmail") built 2026-08-27, iterated and confirmed working end-to-end live 2026-08-28 (a real submit-time bug was hit and fixed, meaning the send path was actually exercised, not just the panel's presence/layout). A20 (merchant "expose" confusion) diagnosed 2026-08-27 as resolved by A25's context-menu Bind-to-Interaction fix — not independently re-confirmed live. A21 remains deferred by Grant.
**Show under test:** JG5X (per Grant, 2026-08-19), venue Catharsis.

---

## 0. Repository audit (spec §33)

Delegated to two research passes before writing any code. Findings that shaped the design:

- **No existing single-Showing Audience admission primitive.** `show_run_tickets` (Kernel 71's two-punch ticket) is Show-Run-scoped, `requested_role` CHECK-locked to `'player'`, and its own migration comment anticipated-but-didn't-build a future Audience widening. Deliberately did **not** widen it — Kernel 93 needs Showing scope (one live Showing), which a Show-Run-scoped table can't express without conflating the two systems (spec §2). Built `audience_admissions` as a new, clearly separate table instead.
- **The actual K90 blocker:** `access.ResolveVisibleVenues`'s Catharsis branch only admits `location_memberships.role IN ('producer','director','cast','crew')`. A plain `audience`-role account with zero location memberships cannot see the Catharsis tile at all, and both the page-load snapshot route and the `/api/session/catharsis/join` route gate on this same function — so this one query is the entire chokepoint for entry.
- **Once past that gate, the rest of the pipeline already works correctly for a pure Audience account with no location membership:** `identity.JoinVenue`'s `resolveRoleForUser` defaults to `"audience"` when no location role is found; `stageobjects.ResolveViewer`'s `Audience = !cast` is already the correct complement regardless of how the viewer got in; `session_participants.role` (not the participation resolver) is what actually reaches the live WS session. This meant the fix could stay small instead of touching the whole role-resolution stack.
- **Kernel 92 Showtime's own selector** (`sessions WHERE show_id = $1 AND status IN ('rehearsal','live') ORDER BY started_at DESC LIMIT 1`) is what "the currently live Showing for a Show" already means everywhere else in this codebase (showtime.Start/Status/End). Every new endpoint reuses this exact query rather than inventing a second live-event selector (spec §25).
- **rollaudience.ModeShow already returns true unconditionally** — i.e. current public dice behavior is "on" — which is why the new Audience Dice Rolls toggle defaults ON (spec §5).
- **No Audience tier exists in `socio.ResolveTier`** — Socio's own doc comment says Audience-facing output is meant to come from "the existing stage/Cave projection pipeline," not the Player/Director mechanical query surface. No qualitative per-character health data pipeline reaches any client today.
- **Presence has no Audience-safe read endpoint** — the existing preview UI reads `/api/director-console/current`, which is Director/Producer-only. No fork of the Presence system was built to work around this (spec §17); see the open item below instead.
- **Reactions (`react/emote`) are fully wired server-side** (`actions/react.go`, `network/ws.go`) but have **zero frontend caller or renderer** anywhere in the repo today. Out of scope for this setup pass — recorded as a Pass B/FUTURE FEATURE candidate, not built here.

---

## 1. Setup work (pre-Pass-A)

| ID | Area | Decision / Action | Files | Status |
|----|------|--------------------|-------|--------|
| S1 | Schema | New `audience_admissions` (user_id, showing_id, issued_by, issued_at) and `audience_projection_configs` (showing_id PK, three booleans, updated_by/at) tables. Kept structurally separate from `show_run_tickets`. | `backend/migrations/110_kernel93_audience_admission.sql` | Done |
| S13 | Deploy | Discovered during first deploy attempt (2026-08-22): the live Hetzner server had substantial uncommitted Kernel 92 work in its working tree (Showtime scheduler, showing nicknames, chat bridge) never pushed to GitHub — including its own uncommitted `109_kernel92_showing_nickname.sql`, colliding with this kernel's `109_kernel93_audience_admission.sql`. Grant committed the server's Kernel 92 work in place first (preserving it) rather than discarding it. Renumbered this kernel's two migrations to 110/111 to resolve the collision; reconciling the two histories via a side branch rather than a live merge on the server. | `backend/migrations/110_kernel93_audience_admission.sql`, `111_kernel93_victory_theater_venue.sql` | Done |
| S2 | Backend | `audienceadmission` package: `IssueForShowCode`/`ListForShowCode`, resolves Show by short code → Show-Run authority (`showruns.CanManageShowRun`) → current live/rehearsal Showing via Showtime's own selector → upserts admission by target handle (handle-paste, matching `storyboards/grants.go`'s established no-user-search convention). | `backend/internal/audienceadmission/*.go` | Done |
| S3 | Backend | `audienceprojection` package: `Config` (dice/presence/health booleans), `ForShowing`/`ForShowCode`/`UpdateForShowCode`, same Showtime-selector + authority pattern. Defaults: dice **ON** (matches current public behavior), presence **OFF**, health **OFF** (Grant's explicit answers, 2026-08-19). | `backend/internal/audienceprojection/*.go` | Done |
| S4 | Backend | Venue-gate fix: added a narrow `audience_admissions`-scoped UNION branch to `access.ResolveVisibleVenues`'s Catharsis query, and a matching Step 3.5 in `participation.ResolveParticipationContext`. Both scoped through the admission's own Showing's still-live session — never a blanket venue grant. | `backend/internal/access/visibility.go`, `backend/internal/participation/resolver.go` | Done |
| S5 | Backend | Confirmed (no code change needed) that `stageobjects.ResolveViewer`'s Cast-complement logic and `identity.JoinVenue`'s role fallback already handle a pure Audience account correctly once S4 lets them past the gate. | — | Verified, no change |
| S6 | Backend | Audience Dice Rolls enforcement: gated Show-mode roll visibility (snapshot re-fetch, pinned-effect reconnect, **and live broadcast delivery**) on the Showing's `show_dice_rolls` toggle, for Audience viewers only, always excluding nothing for the roller's own roll. Three call sites (`world/snapshot.go`, `network/ws.go` ×2) each carry a small local duplicate of the config read rather than importing `audienceprojection`, to avoid an import cycle (`network`→`audienceprojection`→`shows`→`network`). | `backend/internal/world/snapshot.go`, `backend/internal/network/ws.go` | Done |
| S7 | Backend | Embedded the resolved `AudienceProjectionConfig` directly into `world.Snapshot` (`audience_config` field) so any client already fetching the venue snapshot gets the current toggle state for free, no second authorized fetch needed. | `backend/internal/world/snapshot.go` | Done |
| S8 | Frontend | Director UI: three toggle buttons (Dice Rolls / Presence / Health Statuses, `chip` button convention, `aria-pressed`) plus a handle-input "Admit" control, added to the existing Showtime card (`show.html`) rather than the legacy `directors-chair` Director Console — confirmed that console is hardcoded to `"the-cave"` venue's session resolution and cannot see Catharsis Showings at all. | `frontend/venues/show-runs/show.html` | Done |
| S9 | Frontend | Audience overlay: new `#audience-drawer` in `catharsis/index.html`, structurally matching the existing left-drawer's drawer/rail language but simpler (Diagnostic / Presence / Health Status only, no Director/Cast controls). New standalone module `kernel93-audience-overlay.js` (independent of runtime.js's internals, own `/api/world/catharsis` poll) hides the Cast/Director drawers and shows this one when the server-resolved `theater_context.kind === "audience"`. | `frontend/venues/catharsis/index.html`, `frontend/lib/stage-runtime/kernel93-audience-overlay.js` | Done, with an open gap (S10) |
| S10 | Frontend | **Open gap, disclosed rather than papered over:** Presence and Health Status overlay sections currently show a static "this is on for the Showing" note, not live per-connection Presence data or live per-character qualitative health. Wiring those needs (a) a Presence read path that doesn't require Director authority, and (b) deciding which characters' health Audience should see and how — both are Pass A/B-shaped product questions (spec §13, §17) better answered after Grant is actually watching, not guessed here. | — | **Deferred to live pass** |
| S11 | Verification | `go build ./...` and `go vet ./...` clean across the whole backend after every change. | — | Done |
| S12 | Verification | **Could not run the DB-backed Go test suite in this environment** — no Docker/Postgres access in this sandbox (`TEST_DATABASE_URL` unavailable). Pre-existing `dbtest`-tagged tests fail with a clear "TEST_DATABASE_URL is required" message, not a regression from this work. Recommend running `TEST_DATABASE_URL=... go test ./...` (see `Construction/kernel-maker-field-guide.md`) before or during the live dress rehearsal. | — | **Not run — needs a real Postgres** |

---

## 2. Setup acceptance gate (spec §34) — self-assessment

1. A Showing exists — yes, once Grant runs Showtime for JG5X.
2. Director can issue/assign Audience admission — yes, S2 + S8 (handle-paste on the Show page).
3. Audience account can enter the correct venue — yes, S4 (needs live verification — see §3 below).
4. Audience account is truly resolved as Audience — yes, S4/S5 (`theater_context.kind == "audience"`, `session_participants.role == "audience"`).
5. Audience sees no Director/Cast-only controls — yes structurally, S9 (Cast/Director drawers are hidden outright for Audience, not CSS-dimmed).
6. Director can toggle Dice/Presence/Health Status — yes, S3 + S8.
7. Audience projection changes accordingly — **Dice: yes (S6, real server-side enforcement, including live broadcast). Presence/Health: toggle persists and gates overlay visibility, but no live data is streamed yet (S10).**
8. Presence begins collapsed if enabled — yes, S9 (native `<details>`, closed by default).

Items 1–6 and dice-in-7 and 8 are real. Presence/health-in-7 is a known, disclosed partial — see S10.

---

## 3. What I could not verify myself

No browser and no live database in this environment, so none of the following has actually been exercised end to end:

- Signing in as an admitted Audience account and confirming the venue actually opens (the SQL is written and mirrors working query shapes elsewhere, but is unexecuted).
- Confirming a Show B admission genuinely cannot enter Show A's Showing live (§28's core security proof) beyond reading the join logic by eye.
- Any visual/UX judgment about the overlay, toggle placement, or whether this is pleasant to watch — that is explicitly Grant's call per spec §0.

This is exactly what Pass A is for. Recommend Grant runs the DB-backed test suite first if convenient, then the live setup acceptance gate (§34) as the very first live action, before starting Pass A.

---

## 4. Decisions recorded (not to be re-asked)

- Test target: a new/dedicated environment is not being stood up — Show **JG5X** is the live target (Grant, 2026-08-19).
- Audience Presence toggle default: **OFF** at Showing creation (Grant, 2026-08-19).
- Audience Health Statuses toggle default: **OFF** at Showing creation (Grant, 2026-08-19).
- Audience Dice Rolls toggle default: **ON**, derived from repository evidence (current public `ModeShow` behavior), per spec §5's instruction to prefer existing behavior over asking.
- (Carried from the kernel spec §32, unchanged.)

---

## 5. Pass A–D

Pass A in progress (2026-08-22 through 2026-08-26), live with Grant + a disposable second Audience account (`test-user-1`, direct DB session insert per the field guide's Two-Browser technique). Entries below are what actually surfaced testing the "Scene transition" and general stage-sync items on Pass A's checklist — most of the pass turned out to be blocked on infrastructure that had never actually been exercised live before, not on Audience-experience polish. Table is a summary; see chat history for full root-cause detail on each.

A22–A27 (2026-08-24 through 2026-08-26) are a separate live-testing round of A13's Configurator Mode specifically, done without direct database or browser access on the fixing side — every entry below was diagnosed by reading code against Grant's live symptom reports, not by reproducing the bug directly. Fixes were verified by Grant's own retesting ("I believe it all works now"), not independently confirmed end-to-end.

| ID | Pass | Observation | Classification | Decision | Action | Status | Files changed |
|----|------|-------------|-----------------|----------|--------|--------|----------------|
| A1 | A | New token placed on stage showed nameplate only, no asset, for Audience | Bug | Fix | `userCanReadAsset`'s role list never included `audience`, no placement-based exception like the existing "map" one; added an analogous exception for publicly-placed tokens | Fixed, confirmed | `backend/internal/assets/read.go` |
| A2 | A | Grid on/off toggle didn't reach Audience | Bug | Fix | Grid save handler had no hub access at all; wired into the existing `venue/update` broadcast | Fixed, believed working (Grant) | `backend/internal/venues/grid.go`, `cmd/victory/main.go`, `frontend/lib/stage-runtime/session-sync.js` |
| A3 | A | Index card text/color edits didn't appear for a receiving client | Bug | Fix | `state.js`'s shared update-handler never refreshed the flat `frontText`/`backText`/`color`/`label` fields rendering actually reads (only nested them under `source.data`); label fell back to the literal string `"Token"` when a payload had no `name` | Fixed, confirmed | `frontend/lib/stage-runtime/state.js` |
| A4 | A | Index card color specifically never persisted, even after A3 | Bug | Fix | `sanitizeIndexCardRequest` eagerly normalized an empty color to the hardcoded default before `updateIndexCard`'s own "preserve existing if none sent" fallback could ever see an empty string | Fixed, confirmed | `backend/internal/actions/indexcard.go` |
| A5 | A | Card flip: flashed briefly on Director's own screen then reverted, never reached Audience | Bug | Fix | Two separate bugs: the in-canvas flip button was entirely unnetworked (local-only), and the raw WS handler never extracted `payload["face"]` into `IndexCardRequest` at all (manual field-mapping, not a generic unmarshal) | Fixed, confirmed | `frontend/lib/stage-runtime/scene-nodes.js`, `runtime.js`, `backend/internal/actions/indexcard.go`, `backend/internal/network/ws.go` |
| A6 | A | "Cartograph" toolbar visible (dimmed) to Audience; also misspelled | Bug/Polish | Fix | Toolbar mount was gated only by a venue capability flag, no role check; Cast+ tool per Grant | Fixed (never mounts for audience; relabeled "Cartography") | `frontend/lib/stage-runtime/runtime.js`, `drawing.js` |
| A7 | A | Audience banner read "You are watching this Show. Player controls are not active." | Polish | Fix | Simplified per Grant's direction | Fixed, confirmed | `backend/internal/world/snapshot.go` + test |
| A8 | A | Removing the venue map reverted to a packaged "Courtyard" default after ~1s instead of staying blank | Bug | Fix | `handleVenueMapGet`'s Catharsis-specific fallback substituted a default whenever no active map row existed; removed, no-map now means blank everywhere, matching every other venue | Fixed, confirmed | `backend/internal/venues/map.go` + test |
| A9 | A | Scene Configuration (Activate/Update/Save as New Scene) had no effect on the live stage at all | Bug (large) | Fix — new sub-kernel | `scene_stage_elements` and the live `elements`/`venue_layout_elements` tables were, and had always been, two disconnected data stores with no bridge between them. Built `CaptureLiveVenueComposition`/`ApplySceneToLiveVenue` (capture the live stage into a Scene; apply a Scene onto the live stage on Activate), wired into Update/Save/Activate. Hardcoded to Catharsis; only the show-wide "Ungrouped" Activate path applies live (a real cohort's Activate still only updates its own pointer, deliberately, to preserve simultaneous per-cohort viewing) | Fixed, confirmed | `backend/internal/scenes/live_bridge.go`, `capture.go`, `composition.go`, `cmd/victory/scene_live_bridge.go`, `main.go`, `backend/internal/shows/http.go` (dead handler removed) |
| A10 | A | Activate/Update appeared to do nothing at all before A9's underlying cause was found | Investigation | — | Traced to the Cohort dropdown defaulting to the first real cohort (not "Ungrouped") whenever any cohort exists on the show — Grant's clicks were hitting the untouched per-cohort pointer the whole time, not the show-wide one | Root-caused, superseded by A9 + A11 | — |
| A11 | A | Scene Configuration required a cohort to do anything | Bug | Fix | "Ungrouped" now falls back to the Show's own `current_show_scene_placement_id` instead of disabling scene management with no cohort selected | Fixed, confirmed | `frontend/lib/stage-runtime/kernel85-cohort-tools.js` |
| A12 | A | "Save as New Scene" gave a confirmation but the result was never reachable — not in the list, no way to load it | Bug | Fix | `save-as-new-scene` only ever created a bare reusable Scene record, never attached it to the Show as a placement; now also creates the placement and activates it immediately | Fixed, confirmed | `frontend/lib/stage-runtime/kernel85-cohort-tools.js` |
| A13 | A | Using Scene Configuration for live-improv testing was found to also change what a brand-new, characterless cast member sees landing in Catharsis (Kernel 74's local-projection protection only activates *after* a Player finishes Ra's dialogue — before that, everyone shares the one `current_show_scene_placement_id`) | Risk, disclosed | Fix — new sub-kernel | Built **Configurator Mode**: a private draft workspace reusing every existing token/card/map/grid tool unchanged (interception at `sendAction()` and at a wrapped `fetch` injected into `editors.js`), so building/switching arrangements never broadcasts to Audience or touches the shared "Ungrouped" projection until an explicit Activate. Added a "New Scene" button (Configurator Mode previously could only open an *existing* Scene's draft) | **Exercised live 2026-08-24–26 — real bugs found, see A22–A27; Grant's assessment after fixes: "I believe it all works now"** | `frontend/lib/stage-runtime/runtime.js`, `kernel85-cohort-tools.js` |
| A14 | A | **Resolved 2026-08-27, per Grant:** the original tutorial Scene's stored composition (Kessa, the door) may have been overwritten by an "Update Current Scene" click made before A10/A11 were understood, while that Scene still held the show-wide pointer | Risk | Fixed | A22/A26/A27 found and fixed the exact class of bug that would cause this (partial edits silently destroying a scene element's own data; "Update Current Scene" discarding a Scene's saved arrangement), and A27 specifically hardens Kessa's self-heal against it | **Grant confirmed live (2026-08-27): Kessa and the door render correctly on placement `70f0d3bb-f097-4cc9-9953-3f06b95d7cda`** — "fairly certain," not re-verified independently, but standing as resolved unless it regresses | — |
| A15 | A | Kessa (a scene-composition element, not a real live element) cannot be removed via the normal stage context menu | Bug, minor | Fix | `logic.js`'s context-menu resolver only ever offered "remove" via `canRemoveLiveStageObject`, which requires `model.live` — and state.js deliberately sets `live: false` for every `scene_composition`-context_class element regardless of whether it has a real `venue_layout_elements` row. The backend's `act/remove_element` already deletes generically by element_id with no context_class check (confirmed by reading `actions/remove.go` + `stage_state.go`), and Configurator Mode already intercepts that exact action type — so the only gap was the menu never offering it. Added `canRemoveSceneCompositionObject` (kind-agnostic, just !locked + authority) and a "Remove from Stage" push in the non-live branch | Fixed, not yet live-verified | `frontend/lib/stage-runtime/logic.js` |
| A16 | A | "Speak with Ra" button appearing in Director view, and disappearing/reappearing on scene activation, looked like stray test code | Not a bug | No action | Confirmed as Kernel 73A/74's real bound-interaction + local-projection tutorial system working as designed, tied to whichever Scene is currently active | Explained, no fix needed | — |
| A17 | A | Director announcements | Checklist item | — | — | **Confirmed working** once enabled for Audience | — |
| A18 | A | Reactions absent for both Audience and Director | Known gap (see §0) | Fix — built (2026-08-26), refined (2026-08-27) | Kernel 1 §8's "Immediate reactions" (clap/boo/cry/smile/laugh/startle/heart/thumbs_up/thumbs_down/cheer/standing_clap) only ever needed a frontend caller and renderer — `react/emote` was already fully wired server-side. Built `reactions.js`: a standalone DOM module (same convention as `kernel89-announcements.js`) with an always-mounted send bar (every role, not Audience-only — the backend already broadcasts to all roles and nothing in spec restricts sending to Audience) and a floating-glyph renderer for incoming reactions, wired through the existing `react/emote` action-stream dispatch in `session-sync.js`. Made the bar collapsible/adjustable and docked it collapsed in the bottom-right corner (2026-08-27) | **Grant: solid, considers this done** | `frontend/lib/stage-runtime/reactions.js`, `session-sync.js`, `runtime.js`, `frontend/venues/catharsis/index.html`, `frontend/venues/first-theater/index.html` |
| A19 | A | Chat visible to Audience with no Director-controlled visibility toggle | Design gap | Fix — built (2026-08-27), UI iterated live (2026-08-28) | Grant wants Audience notes routed to the Director in per-session batches rather than live chat, with local mailbox delivery as the main design (never contingent on the Discord Chat Bridge being connected) and Discord export as an optional extra copy. Kernel 11's `POST /api/note-cards` is hardcoded to the Cave (`identity.ResolveActiveCaveSessionID`, `venue_slug="the-cave"`), so built a narrow Catharsis-scoped sibling instead of widening it (same reasoning as `audienceadmission`'s own doc comment): new `audiencenotes` package resolves the live Catharsis session, confirms the sender is a genuine `session_participants` row in it, and reuses `messages.resolveNoteCardRecipient`'s exact "earliest-seated director" fallback as recipient — a plain durable row in the same `messages` table Kernel 11 already uses (`message_type = "audience_note"`), so the existing `GET /api/messages` mailbox needed zero changes to show it. Added a "Send Fanmail" panel to the Audience overlay (`kernel93-audience-overlay.js`), and a batched-by-session panel + "Export batch to Discord" button in the mailbox (`network.HandleExportAudienceNotesToDiscord`, reusing the existing Discord Chat Bridge thread-lookup/posting helpers, soft-failing to "stayed local only" if no bridge is connected — never blocking delivery). Deliberately no export-dedup bookkeeping: re-exporting the same batch is a visible minor nuisance, not worth a migration. Live on 2026-08-28: found and fixed a real pre-existing bug where the whole Audience overlay was unreachable (no CSS position, no hover-wiring), then through several rounds Grant redocked the panel by the reactions bar, made it start collapsed with a real click-to-toggle, hid Audience's `#top-bar` and chat panel entirely, and relabeled it "Send Fanmail" with the explanatory copy trimmed down to just the message field and send button | **UI confirmed live and iterated to its current shape by Grant; the end-to-end proof (a submitted note actually reaching the Director's mailbox) was never explicitly confirmed in that back-and-forth** — Grant's overall Pass A verdict (see header) covers this, but that specific delivery path hasn't been called out by name | `backend/internal/audiencenotes/notes.go`, `backend/internal/network/audience_notes_export.go`, `backend/cmd/victory/main.go`, `frontend/lib/stage-runtime/kernel93-audience-overlay.js`, `frontend/venues/catharsis/index.html`, `frontend/mailbox/index.html` |
| A20 | A | Merchant "expose" flow confusing / didn't result in a visible, clickable merchant | Investigation pending | Root-caused | Re-read the Merchant panel's "Expose" action (`kernel89-director-tools.js`): it only ever creates a `participant_interaction` record (optionally Cohort-scoped) — it was never meant to conjure a visible token by itself. Kernel 89's own dress rehearsal (`kernel-89-reportback.md`) confirms exposure-plus-stock-visibility was tested and passing at that level. The missing link was attaching that interaction to an actual stage token so it becomes clickable — before A25 (2026-08-24), that only existed as a `window.prompt`-driven button buried in the Scene Configuration panel, not the obvious right-click path a Director would try first. A25 added "Bind to Interaction" to the token/card context menu itself, listing every interaction for the placement (merchants included, unfiltered by type) — so the full flow is now: Expose the merchant, place/select an NPC token in Configurator Mode, right-click it → Bind to Interaction → pick the merchant by name | **Believed resolved as a side effect of A25 — not independently re-confirmed live**; full flow is Configurator-Mode-only (`configuratorBindElementToInteraction` no-ops outside it) | — |
| A21 | A | Left Audience panel (presence/audio/speaking/reactions/one-way messaging) mostly placeholder | Known gap | Deferred by Grant | Explicitly deferred until Discord is wired better in a later pass, not part of Pass A | Deferred by Grant | — |
| A22 | A | Dragging or otherwise editing a token in Configurator Mode silently destroyed its own asset identity — it would render with the generic placeholder art and a raw element-id string as its label ("random string gravestone tokens"), as if a new broken token had replaced it | Bug (large, root cause) | Fix | `UpdateStageElement` replaced a stage element's entire `data` JSONB column with whatever partial fragment the caller sent, instead of merging onto the existing data. Every real Configurator caller sends a genuinely partial fragment by design (a drag-move only carries `venue_slug/layer/snap_mode/token_layer/scale`, a card flip only `front_text/back_text/color/face`), so any edit after the initial create wiped `asset_id`/`asset_content_url`/`asset_name`. Almost certainly the actual mechanism behind A14's suspected data loss, not just a Configurator-Mode-specific bug | Fixed, Grant confirmed | `backend/internal/scenes/composition.go` |
| A23 | A | Tokens placed via Configurator Mode rendered with generic `construction.png` art instead of the chosen Warehouse asset, even though the token picker's own preview showed the correct art | Bug | Fix | The Configurator's create/token and update/token REST path never resolved `asset_id` → `asset_content_url`/`asset_name`/etc server-side the way the live create/token action stream already does (`token.go`'s `StoreCreateToken`); it stored only `asset_id`. Added `actions.ResolveTokenAssetFields`, called from both `scenes.createStageElement` and `UpdateStageElement` | Fixed, Grant confirmed | `backend/internal/actions/token.go`, `backend/internal/scenes/composition.go` |
| A24 | A | "Place Hotspot" prompted for a label, appeared to succeed, but nothing ever showed up on the Configurator canvas | Bug | Fix | Two stacked bugs: (1) the Configurator's composition loader filtered `interaction_hotspot` elements out entirely, building no renderable model at all; (2) once added, the hand-built hotspot model was missing `source.context_class = "scene_composition"`, which `geometry.js` requires to read a hotspot's `{x,y}` as a 0-1 fraction of the stage rather than literal pixels — without it, every hotspot rendered at pixel (0.5, 0.5), one pixel from the top-left corner, invisible behind the header | Fixed, Grant confirmed | `frontend/lib/stage-runtime/runtime.js` |
| A25 | A | "Bind Selected to Interaction" only existed as a Scene Configuration panel button; right-clicking the token itself (the natural place to look, matching Rename) showed no such option | Bug/Clarity | Fix | Added "Bind to Interaction" to the token/card context menu, parameterized by the right-clicked element rather than the ambient selection — opening a context menu only sets `contextMenuTarget`, it never updates `currentSelection`, so a naive reuse of the panel button's logic could have bound the wrong element | Fixed | `frontend/lib/stage-runtime/logic.js`, `action-router.js`, `runtime.js` |
| A26 | A | "Update Current Scene" reads like "save my Configurator edits" but actually captures the live stage and replaces the Scene's entire saved Base-layer arrangement with it — clicking it right after Building discarded a freshly bound hotspot and a freshly replaced token in one shot | Design gap / data-loss risk | Fix | Same underlying capture semantics as A9's `CaptureLiveVenueComposition`, working exactly as designed — the bug was purely that nothing warned a Director this is destructive and reads the opposite of "save." Added a confirmation dialog spelling out the semantics before it fires | Fixed | `frontend/lib/stage-runtime/kernel85-cohort-tools.js` |
| A27 | A | Kessa's shipped static-file art (`/assets/Kessa.png`) was at risk of being silently overwritten by warehouse-asset fields left behind from an old "Replace Asset" click on her stall | Bug risk | Fix | The self-healing repair in `PrepareLockedCourtyardOpening` reset `asset_content_url`/`asset_thumbnail_url` on every run but never cleared a stray `asset_id` if one had been set — and A23's new server-side resolution means any later token edit would re-derive `asset_content_url` from that leftover `asset_id`, dragging her back to the wrong art even immediately after a repair. Repair SQL now also strips `asset_id` and its related warehouse-only fields | Fixed | `backend/internal/merchant/prepare.go` |
| A28 | A→B | Producer account (backstage, `theater_context.kind` confirmed `"backstage"`) could not see Kessa or the locked door on the live Catharsis stage at all, even though the `scene_stage_elements` rows for both were confirmed present in the DB | Bug (root cause) | Fix | Every gate/canonical-visibility check traced correctly for backstage. Root cause: the Producer account carried a stray `show_cohort_assignments` row (Kernel 85), and `loadSceneCompositionAsPlacedElements` substitutes a cohort-assigned viewer's *own* cohort scene for the Show's shared one with no role exemption — so Producer's composition query was silently pointed at an unrelated scene the whole time. Backstage roles (Producer/Director/Operator/Crew) now exempted from the cohort-placement override | Fixed, Grant confirmed live | `backend/internal/world/snapshot.go` |
| B1 | B | Collapsed "Send Fanmail" Audience drawer had a visible dark curve cutting through the S, later diagnosed as the shared 18px panel radius + zero collapsed padding reading as a pill shape whose rounded end sat right against the un-padded label | Polish (took 3 iterations to actually land) | Fix | First attempt (strip the background's radial-gradient layer) didn't touch it — the curve was geometric, not a gradient artifact. Second attempt (flatten opacity/blur/border to "solid") fixed the curve but exposed the panel's normal frame with no border to soften it, looking like a mismatched hard-edged button. Final fix: smaller regular-button radius, real padding, label centered on both axes, arrow hidden until open | Fixed, Grant confirmed | `frontend/venues/catharsis/index.html` |
| C1 | C | Late-arrival campus tour force-routed to Catharsis before the user actually had permission, then immediately fired the Greenroom pin before character creation had even started | Design gap | Fix — new sub-kernel entry (per Grant, 2026-08-30) | `campus_continuation` bundled the Catharsis pin and Greenroom pin as one atomic tour, eligible the instant `campus_mandatory` finished, with no check for actual Catharsis access or character-creation state. Split the Greenroom pin into its own `greenroom_intro` tour key (migration 112): the Catharsis pin now requires real Catharsis access (same access_grants/audience_admissions condition the map itself uses for tile visibility); `greenroom_intro` requires an owned `character_cards` row (reusing the Greenroom tile's own visibility condition) | **Fixed, confirmed live** — Pass C completed 2026-08-30, per Grant | `backend/internal/tour/{tour,definitions,eligibility}.go`, `backend/migrations/112_kernel93_tour_greenroom_split.sql`, `frontend/app.js`, `frontend/account/index.html` |

---

## 6. Kernel closure — Pass B, C, D complete (2026-08-30)

Per Grant, 2026-08-30 ("I just finished End, everything is green, 93 passes."): Passes B, C, and D were all run live and completed in the same session as this verdict. Per Grant's own characterization: Pass B turned up very few bugs, Pass C fewer still, and Pass D turned up none at all — partly because the deeper features Pass D's checklist gestures at (e.g. Showing Review and other richer post-Showing surfaces) are explicitly out of scope for Kernel 93 and belong to later kernels, so there was less surface for D to find bugs in.

- **Pass B ("Is it enjoyable to watch?")** — visual hierarchy, stage dominance, chat/reaction behavior, left overlay weight, Presence behavior, roll presentation, announcements, Scene transitions, Character identity clarity, and excess chrome/debug-looking surfaces all confirmed acceptable live. B1 (the collapsed "Send Fanmail" drawer shape, §5) was the main fix; Grant characterizes the rest of the pass as turning up very few bugs beyond that.
- **Pass C ("Late arrival and reconnect")** — the full test protocol (join after Showtime already began, reload, disconnect/reconnect, leave/return venue, Scene changes while disconnected) was exercised live: correct Showing, correct Scene, correct public objects, no hidden-state leak, projection toggles still applying, sensible Presence state, and understandable chat/reaction continuity were all confirmed. C1 (above) was the only bug found across the pass — fewer than Pass B — and its fix held up under live re-verification.
- **Pass D ("Ending")** — End Showtime was tested live: what Audience sees immediately before/at the end, that the stage/chat did not vanish abruptly without explanation, that Audience understood the Showing had concluded, and that a sensible route away/back existed, all read correctly. No bugs found — and per Grant, some of what a deeper Ending pass could probe (richer post-Showing surfaces) is deliberately deferred to a later kernel rather than in scope here, which is part of why this pass came back clean.

This closes Kernel 93 against spec §35's pass criteria in full — Passes A through D are each complete, decisions recorded in §4 stand without being reopened, and the Audience experience is coherent enough for Grant to watch a performance rather than operate the software.

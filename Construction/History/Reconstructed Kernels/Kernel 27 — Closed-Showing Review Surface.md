# Kernel 27 — Closed-Showing Review Surface

**Provenance:** RECONSTRUCTED — PARTIAL
**Original date/window:** Unrecoverable precisely; falls between Kernel 24 (Greenroom character dressing) and Kernel 28 (live Director Console), so in the same pre-Kernel-32 window (before Discord OAuth, i.e. before roughly early-to-mid 2026-05 based on Kernel 25's 2026-05-06 commit)
**Implementation status:** IMPLEMENTED — status not recorded (no surviving spec or reportback)
**Evidence sources:** `Construction/kernel-maker-field-guide.md:48` — the sole surviving reference: "Kernel 27 added the closed-showing review surface in the Director's Chair."

## Reconstructed purpose

Add a review surface inside the Director's Chair venue for looking back at a Showing after it closed — the historical predecessor to what later became formal Showing/Show-Run review tooling.

## What evidence proves was built

Only the one-sentence field-guide summary survives as direct evidence. No commit, migration, or reportback naming "Kernel 27" was found despite a targeted search across commit messages (`git log --all --grep`), code comments, and migration files.

## Files/systems affected

Unknown — the Director's Chair venue existed by this point (referenced independently elsewhere), but no specific file could be attributed to this kernel's introduction within this pass's timebox.

## Known deviations / later corrections

Unknown. Given how much the Show/Showing model was later reworked (Kernel 66's Show Run/roster model, Kernel 71's Showtime, Kernel 92's Showtime Composition & Showing Scheduler), it is likely this early review surface was substantially superseded, but no direct evidence of that supersession was found to cite here rather than assume.

## What this kernel handed to the next kernel

Unknown beyond the field guide's implication that Kernel 28 (live Director Console) followed it as the "current showing" counterpart to this "closed showing" surface.

## Confidence / unresolved gaps

PARTIAL confidence — the kernel's existence and one-sentence purpose are confirmed by a single authoritative source (the field guide), but no implementation evidence (commit, file, migration) could be independently attributed to it. If a future kernel needs to know exactly what this surface looked like or whether it survives in any form today, that requires a dedicated deeper investigation (e.g., tracing Director's Chair venue history commit-by-commit) beyond this pass's scope.
